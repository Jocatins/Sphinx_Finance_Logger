"use strict";
//Type Alias
const gypsyDetails = (uid, item) => {
    console.log(`${item} has a uid of ${uid}`);
};
const titanUser = (user) => {
    console.log(`${user.name} says hello`);
};
const links = (user) => {
    console.log(`${user.name} says hi`);
};
